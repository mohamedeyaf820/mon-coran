diff --git a/src/styles/mushaf-page-polish.css b/src/styles/mushaf-page-polish.css
index 572308f..25fb7bc 100644
--- a/src/styles/mushaf-page-polish.css
+++ b/src/styles/mushaf-page-polish.css
@@ -264,5 +264,6 @@ html body .app-root[data-view="reading"] .quran-display--platform:has(.quran-mod
   margin-top: 0.32rem;
   color: #d8c9a7 !important;
-  font-size: 0.54rem;
+  /* Rem-only scales fell to 8.1px on a phone (root is 15px while reading). */
+  font-size: clamp(max(0.62rem, 10px), 0.6rem + 0.12vw, 0.72rem);
   letter-spacing: 0.18em;
   text-shadow: 0 1px 1px rgba(0, 0, 0, 0.24);
@@ -576,5 +577,7 @@ html body .app-root[data-view="reading"] .quran-display--platform:has(.quran-mod
   color: var(--reader-page-muted, var(--text-muted)) !important;
   font-family: var(--font-ui, sans-serif) !important;
-  font-size: clamp(0.48rem, 0.4rem + 0.15vw, 0.6rem) !important;
+  /* Measured 7.2px on a phone: the reading root font-size is 15px, so this
+     rem-only clamp fell under the app's 10px legibility floor. */
+  font-size: clamp(max(0.62rem, 10px), 0.6rem + 0.12vw, 0.72rem) !important;
   font-weight: 800 !important;
   letter-spacing: 0.16em !important;
@@ -773,5 +776,13 @@ html body .app-root[data-view="reading"] .quran-display--platform:has(.quran-mod
   border: var(--mfp-frame-outer) solid var(--mushaf-gold-deep);
   border-radius: 0.15rem;
-  background: var(--reader-page-paper);
+  /* Warm paper with a faint gilded vignette toward the edges, like the
+     Madinah folios. Decoration only — the ink grid is untouched. */
+  background:
+    radial-gradient(
+      140% 100% at 50% 45%,
+      transparent 62%,
+      color-mix(in srgb, var(--mushaf-gold) 8%, transparent) 100%
+    ),
+    var(--reader-page-paper);
   box-shadow: var(--reader-page-shadow);
   padding: 0.75rem 0.7rem 0.9rem;
@@ -779,18 +790,19 @@ html body .app-root[data-view="reading"] .quran-display--platform:has(.quran-mod
 }
 
-/* Inner frame, painted behind the ink (same reasoning as the portal). */
+/* Inner frame, painted behind the ink (same reasoning as the portal). The
+   printed Madani folio rules its inner line a clear few points inside the
+   outer one, so the gap reads as a band, not as a double-stroke border. */
 .quran-mode-pane--mushaf .qcm-page::before {
   content: "";
   position: absolute;
   z-index: -1;
-  inset: 5px;
-  border: var(--mfp-frame-inner) solid var(--mushaf-gold-soft);
+  inset: 7px;
+  border: var(--mfp-frame-inner) solid color-mix(in srgb, var(--mushaf-gold) 72%, var(--mushaf-gold-soft));
   border-radius: 0.1rem;
-  box-shadow:
-    inset 0 0 0 3px var(--reader-page-paper),
-    inset 0 0 0 4px color-mix(in srgb, var(--mushaf-gold) 30%, transparent);
   pointer-events: none;
 }
 
+/* Corners: the print closes the double frame with small filled squares at
+   the inner-line junctions, not with floating lozenges. */
 .quran-mode-pane--mushaf .qcm-page .qcm-corner {
   position: absolute;
@@ -799,6 +811,5 @@ html body .app-root[data-view="reading"] .quran-display--platform:has(.quran-mod
   height: var(--mfp-corner-size);
   border: 1px solid var(--mushaf-gold-deep);
-  background: var(--reader-page-paper);
-  transform: rotate(45deg);
+  background: color-mix(in srgb, var(--mushaf-gold) 55%, var(--reader-page-paper));
   pointer-events: none;
 }
@@ -808,11 +819,21 @@ html body .app-root[data-view="reading"] .quran-display--platform:has(.quran-mod
   position: absolute;
   inset: 2px;
-  border: 1px solid var(--mushaf-gold-soft);
+  border: 1px solid var(--reader-page-paper);
+  background: color-mix(in srgb, var(--mushaf-gold) 40%, var(--mushaf-gold-deep));
 }
 
-.quran-mode-pane--mushaf .qcm-page .qcm-corner--tl { top: 3px; left: 3px; }
-.quran-mode-pane--mushaf .qcm-page .qcm-corner--tr { top: 3px; right: 3px; }
-.quran-mode-pane--mushaf .qcm-page .qcm-corner--bl { bottom: 3px; left: 3px; }
-.quran-mode-pane--mushaf .qcm-page .qcm-corner--br { bottom: 3px; right: 3px; }
+/* Lighter pip at the heart of each corner square. */
+.quran-mode-pane--mushaf .qcm-page .qcm-corner::before {
+  content: "";
+  position: absolute;
+  z-index: 1;
+  inset: 38%;
+  background: var(--reader-page-paper);
+}
+
+.quran-mode-pane--mushaf .qcm-page .qcm-corner--tl { top: calc(7px - var(--mfp-corner-size) / 2 + 0.5px); left: calc(7px - var(--mfp-corner-size) / 2 + 0.5px); }
+.quran-mode-pane--mushaf .qcm-page .qcm-corner--tr { top: calc(7px - var(--mfp-corner-size) / 2 + 0.5px); right: calc(7px - var(--mfp-corner-size) / 2 + 0.5px); }
+.quran-mode-pane--mushaf .qcm-page .qcm-corner--bl { bottom: calc(7px - var(--mfp-corner-size) / 2 + 0.5px); left: calc(7px - var(--mfp-corner-size) / 2 + 0.5px); }
+.quran-mode-pane--mushaf .qcm-page .qcm-corner--br { bottom: calc(7px - var(--mfp-corner-size) / 2 + 0.5px); right: calc(7px - var(--mfp-corner-size) / 2 + 0.5px); }
 
 .quran-mode-pane--mushaf :is(.qcm-page-header, .qcm-page-footer) {
@@ -828,9 +849,18 @@ html body .app-root[data-view="reading"] .quran-display--platform:has(.quran-mod
 }
 
+/* The running head sits in a cartouche — the gilded band the Madani print
+   closes around the juz and surah line — instead of resting on a bare rule. */
 .quran-mode-pane--mushaf .qcm-page-header {
   justify-content: space-between;
   gap: clamp(0.4rem, 1vw, 0.7rem);
-  padding-bottom: 0.4rem;
-  border-bottom: 1px solid color-mix(in srgb, var(--mushaf-gold) 35%, transparent);
+  margin-top: 0.1rem;
+  padding: 0.16rem 0.9rem;
+  border: 1px solid color-mix(in srgb, var(--mushaf-gold) 62%, transparent);
+  border-radius: 999px;
+  background: linear-gradient(
+    to bottom,
+    color-mix(in srgb, var(--mushaf-gold) 5%, transparent),
+    color-mix(in srgb, var(--mushaf-gold) 11%, transparent)
+  );
 }
 
@@ -842,5 +872,5 @@ html body .app-root[data-view="reading"] .quran-display--platform:has(.quran-mod
 
 .quran-mode-pane--mushaf .qcm-page-header__meta {
-  color: var(--reader-page-muted);
+  color: color-mix(in srgb, var(--mushaf-gold-deep) 55%, var(--reader-page-muted));
   font-size: 0.68rem;
   font-weight: 600;
@@ -859,10 +889,27 @@ html body .app-root[data-view="reading"] .quran-display--platform:has(.quran-mod
 }
 
+/* Twin gilled lozenges flanking the surah name — the running-head ornament
+   of the Madinah folios. Pseudo-elements sit inside the name box, so a very
+   long name simply clips them before it ever overflows the sheet. */
+.quran-mode-pane--mushaf .qcm-page-header__name::before,
+.quran-mode-pane--mushaf .qcm-page-header__name::after {
+  content: "";
+  display: inline-block;
+  width: 0.28rem;
+  height: 0.28rem;
+  margin-inline: 0.42rem;
+  vertical-align: 0.12em;
+  transform: rotate(45deg);
+  border: 1px solid color-mix(in srgb, var(--mushaf-gold) 75%, transparent);
+  background: color-mix(in srgb, var(--mushaf-gold) 30%, transparent);
+}
+
+/* The folio is the printed page's quiet number: the Madani mushaf sets it in
+   a small rosette below the text block, without a rule above it. */
 .quran-mode-pane--mushaf .qcm-page-footer {
   justify-content: center;
   gap: 0.6rem;
-  margin-top: 0.75rem;
-  padding-top: 0.45rem;
-  border-top: 1px solid color-mix(in srgb, var(--mushaf-gold) 35%, transparent);
+  margin-top: 0.6rem;
+  padding-top: 0.1rem;
 }
 
@@ -871,18 +918,5 @@ html body .app-root[data-view="reading"] .quran-display--platform:has(.quran-mod
   align-items: center;
   justify-content: center;
-  min-width: 2.5rem;
-  height: 2.5rem;
-  border: 1.5px solid var(--mushaf-gold-deep);
-  border-radius: 50%;
-  background: var(--reader-page-paper);
-  color: color-mix(in srgb, var(--mushaf-gold) 75%, var(--reader-page-ink));
-  font-family: var(--font-ui, sans-serif);
-  font-size: 0.72rem;
-  font-weight: 750;
   line-height: 1;
-  box-shadow:
-    inset 0 0 0 3px var(--reader-page-paper),
-    inset 0 0 0 5px color-mix(in srgb, var(--mushaf-gold) 28%, transparent),
-    0 0 0 7px color-mix(in srgb, var(--mushaf-gold) 10%, transparent);
 }
 
@@ -896,5 +930,5 @@ html body .app-root[data-view="reading"] .quran-display--platform:has(.quran-mod
   margin-inline: auto;
   margin-top: 0.4em;
-  font-size: clamp(14px, calc((100cqi - 3rem) / 16.7), 34px);
+  font-size: clamp(14px, var(--qcm-mushaf-font-size, 34px), calc((100cqi - 3rem) / 16.7));
   line-height: 1;
   grid-template-rows: repeat(15, var(--mfp-line-pitch));
@@ -930,15 +964,55 @@ html body .app-root[data-view="reading"] .quran-display--platform:has(.quran-mod
   justify-content: center;
   width: 100%;
-  height: 1.45em;
+  height: 1.55em;
   border: 1px solid color-mix(in srgb, var(--mushaf-gold) 80%, transparent);
+  border-radius: 0.9em;
+  background: linear-gradient(
+    to bottom,
+    color-mix(in srgb, var(--mushaf-gold) 6%, var(--reader-page-paper)),
+    color-mix(in srgb, var(--mushaf-gold) 14%, var(--reader-page-paper))
+  );
   box-shadow:
-    inset 0 0 0 3px var(--reader-page-paper),
-    inset 0 0 0 4px color-mix(in srgb, var(--mushaf-gold) 55%, transparent);
+    inset 0 0 0 2.5px var(--reader-page-paper),
+    inset 0 0 0 3.5px color-mix(in srgb, var(--mushaf-gold) 55%, transparent);
   color: var(--reader-page-ink);
 }
 
+/* Gilded diamonds close the band at each end of the name. */
+.quran-mode-pane--mushaf .qcm-surah-title::before,
+.quran-mode-pane--mushaf .qcm-surah-title::after {
+  content: "";
+  flex: 0 0 auto;
+  width: 0.22em;
+  height: 0.22em;
+  margin-inline: 0.55em;
+  background: color-mix(in srgb, var(--mushaf-gold) 85%, var(--mushaf-gold-deep));
+  transform: rotate(45deg);
+  opacity: 0.85;
+}
+
+/* Readable name + numbered rosette, mirroring the immersive band: the
+   "surahnames" digit ligature read as ornament, not as the surah's name. */
 .quran-mode-pane--mushaf .qcm-surah-title__name {
-  font-family: "surahnames", serif;
-  font-size: 1.1em;
+  font-family: var(--font-quran, "Amiri Quran"), serif;
+  font-size: 1.05em;
+  line-height: 1.2;
+  color: var(--mushaf-gold-deep);
+}
+
+.quran-mode-pane--mushaf .qcm-surah-title__num {
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  flex: 0 0 auto;
+  min-width: 1.3em;
+  height: 1.3em;
+  margin-inline-start: 0.15em;
+  border: 1px solid color-mix(in srgb, var(--mushaf-gold) 70%, transparent);
+  border-radius: 50%;
+  background: color-mix(in srgb, var(--mushaf-gold) 10%, var(--reader-page-paper));
+  color: var(--mushaf-gold-deep);
+  font-family: var(--font-quran, "Amiri Quran"), serif;
+  font-size: max(0.58em, 10px);
+  font-weight: 700;
   line-height: 1;
 }
@@ -950,9 +1024,132 @@ html body .app-root[data-view="reading"] .quran-display--platform:has(.quran-mod
 }
 
+/* Illuminated opening leaves (pages 1-2): mirrors the portal treatment so
+   the inline stream and the immersive book print the same frame. Decoration
+   only — it is painted behind the ink and carries no text. */
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-opening-frame {
+  position: absolute;
+  inset: 0;
+  z-index: -1;
+  pointer-events: none;
+  color: color-mix(in srgb, var(--mushaf-gold) 72%, var(--mushaf-gold-deep));
+}
+
+/* Same promotion trap as the portal: the leaf must own its stacking context
+   for the frame to paint on the paper. */
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-page {
+  isolation: isolate;
+}
+
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-page .qcm-corner {
+  display: none;
+}
+
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-page::before {
+  content: none;
+}
+
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] :is(.qcm-page-header, .qcm-page-footer) {
+  border-color: transparent;
+}
+
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-page-header {
+  padding-top: 0.45rem;
+}
+
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-opening-frame__panel {
+  position: absolute;
+  inset: 0.32rem 0.32rem 0.3rem;
+  border: 1px solid currentColor;
+  border-radius: 0.4rem;
+  box-shadow:
+    inset 0 0 0 2px var(--reader-page-paper),
+    inset 0 0 0 3px color-mix(in srgb, var(--mushaf-gold) 34%, transparent);
+}
+
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-opening-frame__rosette {
+  position: absolute;
+  width: 0.7rem;
+  height: 0.7rem;
+  background: var(--reader-page-paper);
+  border-radius: 50%;
+  padding: 1px;
+}
+
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-opening-frame__rosette--tl {
+  top: 0;
+  left: 0;
+  transform: translate(-50%, -50%);
+}
+
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-opening-frame__rosette--tr {
+  top: 0;
+  right: 0;
+  transform: translate(50%, -50%);
+}
+
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-opening-frame__rosette--bl {
+  bottom: 0;
+  left: 0;
+  transform: translate(-50%, 50%);
+}
+
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-opening-frame__rosette--br {
+  bottom: 0;
+  right: 0;
+  transform: translate(50%, 50%);
+}
+
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-surah-title {
+  width: min(100%, 13.5em);
+  border-width: 1.5px;
+  border-radius: 1.1em;
+}
+
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-surah-title::before,
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-surah-title::after {
+  content: "";
+  flex: 0 0 auto;
+  width: 0.3em;
+  height: 0.3em;
+  margin-inline: 0.9em;
+  background: color-mix(in srgb, var(--mushaf-gold) 85%, var(--mushaf-gold-deep));
+  transform: rotate(45deg);
+  opacity: 0.9;
+}
+
+/* A short opening page keeps the fifteen-line leaf height but spreads its
+   lines evenly through the panel instead of stacking them against the
+   running head. */
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-lines:not([data-warsh="true"]) {
+  display: flex;
+  flex-direction: column;
+  justify-content: space-evenly;
+  min-height: 24.3em;
+}
+
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-lines[data-warsh="true"] {
+  display: flex;
+  flex-direction: column;
+  justify-content: center;
+  min-height: 24.3em;
+}
+
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-lines[data-warsh="true"] .qcm-flow {
+  margin-inline: 0.55em;
+}
+
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-lines:not([data-warsh="true"]) .qcm-line {
+  min-height: var(--mfp-line-pitch);
+}
+
+.quran-mode-pane--mushaf .qcm-page-shell[data-page-kind="opening"] .qcm-line--empty {
+  display: none;
+}
+
 /* Warsh: one continuous justified body over the same row pitch. */
 .quran-mode-pane--mushaf .qcm-lines[data-warsh="true"] {
   display: block;
   width: var(--mfp-line-measure);
-  font-size: clamp(14px, calc((100cqi - 3rem) / 16.7), 34px);
+  font-size: clamp(14px, var(--qcm-mushaf-font-size, 34px), calc((100cqi - 3rem) / 16.7));
   line-height: var(--mfp-line-pitch);
 }
@@ -1001,4 +1198,16 @@ html body .app-root[data-view="reading"] .quran-display--platform:has(.quran-mod
 }
 
+/* Rule-based Warsh tajweed: the colour is carried per whole word by the
+   renderer (sub-word ranges slice cursive ink). :where keeps the selector at
+   the base specificity so the hover/active/focus/playing rules below still
+   win over the ink. */
+.quran-mode-pane--mushaf .qcm-word:where([data-tajwid]) {
+  color: color-mix(
+    in srgb,
+    var(--qcm-word-tajwid, var(--reader-page-ink)) 48%,
+    var(--reader-page-ink)
+  );
+}
+
 .quran-mode-pane--mushaf :is(.qcm-word:hover, .qcm-word--active) {
   color: var(--mushaf-gold-deep);
@@ -1022,9 +1231,75 @@ html body .app-root[data-view="reading"] .quran-display--platform:has(.quran-mod
 }
 
-.quran-mode-pane--mushaf .qcm-line :is(.qcm-ayah-marker, .ayah-marker, .ayat-marker) {
+/* reading-platform.css resets every .native-ayah-marker to margin 0
+   !important under .app-root; out-specify it inside the page stream so the
+   medallion hugs the last word of its own ayah (start) and leaves a clear
+   gap before the next ayah (end). */
+html body .app-root .quran-mode-pane--mushaf .qcm-line
+  :is(.qcm-ayah-marker, .ayah-marker, .ayat-marker) {
   flex: 0 0 auto;
-  margin-inline: 0.18em;
+  display: inline-flex;
+  align-items: center;
+  vertical-align: middle;
+  margin-inline: 0.1em 0.7em !important;
   min-width: 0;
   min-height: 0;
   overflow: visible;
 }
+
+/* The khatam rosette — shared by the inline sheet and the fullscreen portal
+   (both render MushafAyahMarker / the folio rosette from MushafPageShell).
+   The ornament is decoration: the number is ink, the gilding stays quiet so
+   the text remains the loudest thing on the page. */
+:is(.quran-mode-pane--mushaf, #root ~ .mfp-portal-root .mfp-book) .qcm-rosette {
+  display: block;
+  width: 1.06em;
+  height: 1.06em;
+  overflow: visible;
+}
+
+:is(.quran-mode-pane--mushaf, #root ~ .mfp-portal-root .mfp-book) .qcm-rosette__ring {
+  fill: var(--reader-page-paper);
+  stroke: color-mix(in srgb, var(--mushaf-gold) 80%, var(--mushaf-gold-deep));
+  stroke-width: 1.1;
+}
+
+:is(.quran-mode-pane--mushaf, #root ~ .mfp-portal-root .mfp-book) .qcm-rosette__disc {
+  fill: none;
+  stroke: color-mix(in srgb, var(--mushaf-gold) 55%, transparent);
+  stroke-width: 0.8;
+}
+
+:is(.quran-mode-pane--mushaf, #root ~ .mfp-portal-root .mfp-book) .qcm-rosette__num {
+  fill: var(--reader-page-ink);
+  font-family: var(--font-ui, system-ui, sans-serif);
+  font-size: 9.5px;
+  font-weight: 700;
+}
+
+/* Juz opening: the printed Mushaf gilds the medallion of the verse a juz
+   begins with, so the boundary reads from across the page. */
+:is(.quran-mode-pane--mushaf, #root ~ .mfp-portal-root .mfp-book) .qcm-ayah-marker--juz .qcm-rosette__ring {
+  fill: color-mix(in srgb, var(--mushaf-gold) 16%, var(--reader-page-paper));
+  stroke: var(--mushaf-gold-deep);
+}
+
+:is(.quran-mode-pane--mushaf, #root ~ .mfp-portal-root .mfp-book) .qcm-ayah-marker--juz .qcm-rosette__num {
+  fill: var(--mushaf-gold-deep);
+}
+
+/* The verse being recited gilds its own khatam, like the word highlight. */
+:is(.quran-mode-pane--mushaf, #root ~ .mfp-portal-root .mfp-book) .qcm-ayah-marker.is-playing .qcm-rosette__ring {
+  stroke: var(--primary);
+  fill: rgba(var(--primary-rgb), 0.1);
+}
+
+/* The folio rosette is set small, like the print's quiet page number. */
+:is(.quran-mode-pane--mushaf, #root ~ .mfp-portal-root .mfp-book) .qcm-rosette--folio {
+  width: 1.7rem;
+  height: 1.7rem;
+}
+
+/* Hafs tajweed comes from the coloured QCF v4 glyphs — the same letter-painted
+   palette Quran.com ships, so it is left untouched here (no CSS filter). The
+   font only colours the relevant letters, which is what keeps it sober; the
+   extra desaturation used to mute it below Quran.com's own rendering. */
